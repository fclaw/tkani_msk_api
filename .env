# PostgreSQL Credentials
POSTGRES_DB=tkani_db
POSTGRES_USER=sonny
POSTGRES_PASSWORD=Ufgwnf7O08axZYlg9Cfw62SxbaVM6GHbP3DB
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# --- Port Configuration ---
# The port the API service will be accessible on from the OUTSIDE world (the host machine).
# You can change this to 80, 443, or any other port.
API_EXTERNAL_PORT=8081

# The port the Haskell/C++ application listens on INSIDE the container.
# This should match what's in your code and your Dockerfile's EXPOSE instruction.
API_INTERNAL_PORT=8080